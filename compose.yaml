name: scrimmy

services:
  bot:
    container_name: scrimmy-bot
    image: ghcr.io/vidhanio/scrimmy:latest
    build: .
    environment:
      DISCORD_BOT_TOKEN: $DISCORD_BOT_TOKEN
      GUILDS: $GUILDS
      CHANNELS: $CHANNELS
      DATABASE_URL: postgresql://$POSTGRES_USER:$POSTGRES_PASSWORD@database:5432/$POSTGRES_DB

  database:
    container_name: scrimmy-database
    image: postgres
    environment:
      POSTGRES_USER: $POSTGRES_USER
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DB: $POSTGRES_DB
    volumes:
      - database:/var/lib/postgresql/data
    ports:
      - 5432:5432
networks:
  default:
    name: scrimmy-network
volumes:
  database:
    name: scrimmy-database
    driver: local
