app = 'scheduletf'
primary_region = 'lax'

[build]
image = 'registry.fly.io/scheduletf:latest'

[env]
PRODUCTION = 'true'

[[mounts]]
destination = '/data'
source = 'scheduletf_data'

[[vm]]
cpu_kind = 'shared'
cpus = 1
memory = '1gb'
